import * as React from 'react';

/**
 * Page displayed when the root isn't found.
 */
export default class NotFoundComponent extends React.Component {
  render(): JSX.Element {
    return <p>404 not found.</p>
  }
}
